// Legacy code, not used in the current version of the app

// const User = require("../models/userTodo");

// const userMiddleware = (req, res, next) => {
//   User.findByPk(1)
//     .then(user => {
//       req.user = user;
//       next();
//     });
// };

// module.exports = userMiddleware;